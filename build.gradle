apply plugin: 'java-base'

apply from: "$rootDir/gradle/check.gradle"

allprojects {
    group = 'com.enonic.xp'

    repositories {
        mavenLocal()
        jcenter()
        maven {
            url 'http://repo.enonic.com/public'
        }
    }

    buildDir = 'target'
}
