apply plugin: 'java-base'

allprojects {
    group = 'com.enonic.xp'
    version = '5.0.0-SNAPSHOT'

    repositories {
        mavenLocal()
        jcenter()
        maven {
            url 'http://repo.enonic.com/public'
        }
    }

    buildDir = 'target'
}
