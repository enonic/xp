coverage:
  status:
    # Fixed target instead of auto (base coverage): 80% keeps a real gate on
    # changed lines while tolerating defensive branches that are impractical to
    # exercise (interrupts, timeouts, StackOverflowError handlers).
    patch:
      default:
        target: 85%
