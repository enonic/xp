apply plugin: 'maven-publish'

publishing {
    publications {
        maven( MavenPublication ) {}
    }
    repositories {
        maven {
            url 'http://repo.enonic.net/artifactory/public'
        }
    }
}
