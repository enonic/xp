package com.enonic.nodb.server;

/**
 * Placeholder for the NoDB gRPC server module. Real content (proto codegen, auth
 * interceptor, dev token issuer) lands in slice-1 step 5.
 */
public final class NodbServer
{
    private NodbServer()
    {
    }
}
