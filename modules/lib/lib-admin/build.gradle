dependencies {
    compileOnly project( ':core:core-api' )
    compileOnly project( ':admin:admin-api' )
    compileOnly project( ':script:script-api' )
    compileOnly project( ':portal:portal-api' )
    implementation project( ':lib:lib-portal' )

    testImplementation project( ':tools:testing' )
}
