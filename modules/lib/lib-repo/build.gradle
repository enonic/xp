dependencies {
    compile project( ':lib:lib-common' )
    compile project( ':lib:lib-value' )
    compileOnly project( ':core:core-api' )
    compileOnly project( ':script:script-api' )

    testImplementation project( ':tools:testing' )

    testImplementation "org.graalvm.truffle:truffle-api:${graalvmVersion}"
}
