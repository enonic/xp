dependencies {
    compileOnly project( ':core:core-api' )
    compileOnly project( ':portal:portal-api' )
    compile project( ':lib:lib-content' )

    testImplementation project( ':tools:testing' )
}
