apply from: "$rootDir/gradle/osgi.gradle"

dependencies {
    compile project( ':core-api' )
    testCompile project( path: ':core-api', configuration: 'testOutput' )
}

bundle {
    instruction 'Bundle-Name', 'Enonic XP Core - Relationship'
}
