apply from: "$rootDir/gradle/osgi.gradle"

dependencies {
    compile project( ':web-api' )
    testCompile project( path: ':web-api', configuration: 'testOutput' )
}

bundle {
    instruction 'Bundle-Name', 'Enonic XP Web - VHost'
}
