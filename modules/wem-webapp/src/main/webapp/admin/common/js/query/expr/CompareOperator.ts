module api.query.expr {

    export enum CompareOperator {
        EQ,
        NEQ,
        GT,
        GTE,
        LT,
        LTE,
        LIKE,
        NOT_LIKE,
        IN,
        NOT_IN
    }
}