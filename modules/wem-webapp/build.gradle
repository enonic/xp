apply plugin: 'war'

dependencies {
    compile project( ':wem-web' )
    providedCompile 'javax.servlet:javax.servlet-api:3.0.1'
}

publishing {
    publications {
        maven {
            from components.web
        }
    }
}
