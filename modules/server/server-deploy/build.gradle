apply from: "$rootDir/gradle/osgi.gradle"

dependencies {
    compile project( ':core:core-api' )
    testCompile project( path: ':core:core-api', configuration: 'testOutput' )

    //TODO Temporary fix. See comment in com.enonic.xp.server.internal.deploy.ApplicationDeployerManager
    compile project( ':admin:admin-api' )
}

bundle {
    instruction 'Bundle-Name', 'Enonic XP Server - Deploy'
}
