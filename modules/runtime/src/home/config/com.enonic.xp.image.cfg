#scale.maxDimension = 8000
#filters.maxTotal = 25
#memoryLimit = 10%
#progressive = jpeg

# Enable WebP/AVIF output through predefined image styles. Requires an ImageMagick executable
# with the corresponding encoders. Empty (the default) disables modern-format encoding.
#encoding.executable = /usr/bin/magick
#encoding.maxConcurrent = 2
#encoding.maxQueue = 8
#encoding.queueTimeoutSeconds = 5
#encoding.timeoutSeconds = 30
#encoding.maxPixels = 40000000
