#scale.maxDimension = 8000
#filters.maxTotal = 25
#memoryLimit = 10%
#progressive = jpeg

# Select ImageIO or ImageMagic independently for each stage.
#decoding.backend = ImageIO
#decoding.maxBytes = 256mb
#transformation.backend = ImageIO
#encoding.backend = ImageIO

# Shared limits for requests using native decoding, transformations or encoding.
#processing.maxConcurrent = 2
#processing.maxQueue = 8
#processing.queueTimeoutSeconds = 5
# Timeout for each native stage, not the complete request.
#processing.timeoutSeconds = 30
#processing.maxPixels = 40000000
