apply from: "$rootDir/gradle/osgi.gradle"

dependencies {
    include project( ":lib:lib-mustache" )
    include project( ":lib:lib-portal" )
    include project( ":lib:lib-auth" )
}

bundle {
    instruction 'Bundle-Name', 'Enonic XP App - Login'
}
