kind: "Task"
description: "Reindex task"
form:
  - type: "TextLine"
    name: "repository"
    label: "Repository ID"
    occurrences:
      min: 1
      max: 1
  - type: "TextLine"
    name: "branches"
    label: "Branches (default: all)"
    occurrences:
      min: 0
      max: 0
  - type: "CheckBox"
    name: "initialize"
    label: "Drop the index before reindexing"
    occurrences:
      min: 0
      max: 1
