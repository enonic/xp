export interface IssueStatsJson {

    openAssignedToMe: number;

    openCreatedByMe: number;

    open: number;

    closed: number;

    closedAssignedToMe: number;

    closedCreatedByMe: number;
}
