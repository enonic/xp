apply from: "$rootDir/gradle/osgi.gradle"

dependencies {
    compile( 'org.jparsec:jparsec:3.0' ) {
        transitive = false
    }
}

jar {
    bnd( 'Bundle-Name': 'JParsec Library',
         'Export-Package': 'org.jparsec.*;version=3.0',
         'Import-Package': '*;resolution:=optional' )
}
