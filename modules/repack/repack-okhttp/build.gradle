apply from: "$rootDir/gradle/osgi.gradle"

dependencies {
    compile 'com.squareup.okhttp:okhttp:2.5.0'
}

jar {
    bnd( 'Bundle-Name': 'OkHttp Library',
         'Export-Package': 'com.squareup.okhttp.*;version=2.5.0,okio.*',
         'Import-Package': '*;resolution:=optional' )
}
