dependencies {
    implementation project( ':core:core-api' )

    testImplementation( testFixtures( project(":core:core-api") ) )
}

jar {
    bundle {
       bnd( 'Bundle-Name': 'Enonic XP Core - Media' )
    }
}
