# Defaults merged under any properties supplied via the com.enonic.xp.storage.nodb PID.
# backend defaults to "elasticsearch" deliberately: if the PID is provisioned but this key
# is left unset (or explicitly reverted), NodbStorageClient refuses to activate rather than
# silently taking over -- see NodbStorageClient's class javadoc.
backend = elasticsearch
nodbEndpoint =
nodbToken =
