/**
 * This function imports another module using CommonJS require specification.
 *
 * @param {String} name name of module
 * @returns {Object} exported module object
 */
function require(name) {
    // Do nothing. Only a skeleton for documentation.
}

/**
 * This function resolves a resource-key from the current module. The name can either
 * be a qualified name or local name.
 *
 * @param {String} name name to resolve
 * @returns {Object} qualified resource key
 */
function resolve(name) {
    // Do nothing. Only a skeleton for documentation.
}

/**
 * Exports of the module.
 *
 * @type {{Object}}
 */
var exports = {};
