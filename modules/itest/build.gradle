abstract class IntegrationBuildService implements BuildService<org.gradle.api.services.BuildServiceParameters.None>
{
}

gradle.sharedServices.registerIfAbsent( "integration", IntegrationBuildService ) {
    maxParallelUsages = 2
}
