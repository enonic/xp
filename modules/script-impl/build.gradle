apply from: "$rootDir/gradle/osgi.gradle"

dependencies {
    compile project( ':script-api' )
    testCompile project( path: ':script-api', configuration: 'testOutput' )
}

bundle {
    instruction 'Bundle-Name', 'Enonic XP Script - Impl'
}
