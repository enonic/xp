dependencies {
    api project( ':core:core-api' )
    api project( ':portal:portal-api' )
}

jar {
    bundle {
        bnd( 'Bundle-Name': 'Enonic XP Admin - Api',
             'Export-Package': 'com.enonic.xp.admin.*' )
    }
}
